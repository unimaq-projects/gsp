import { DetailedInfoEntity } from './detailed-info.entity';

describe('DetailedInfoEntity', () => {
  it('should create an instance', () => {
    expect(new DetailedInfoEntity()).toBeTruthy();
  });
});
