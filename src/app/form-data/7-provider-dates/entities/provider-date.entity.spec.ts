import { ProviderDateEntity } from './provider-date.entity';

describe('ProviderDateEntity', () => {
  it('should create an instance', () => {
    expect(new ProviderDateEntity()).toBeTruthy();
  });
});
