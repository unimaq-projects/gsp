import { EquipmentInfoEntity } from './equipment-info.entity';

describe('EquipmentInfoEntity', () => {
  it('should create an instance', () => {
    expect(new EquipmentInfoEntity()).toBeTruthy();
  });
});
