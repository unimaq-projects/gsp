import { WorkorderMapEntity } from './workorder-map.entity';

describe('WorkorderMapEntity', () => {
  it('should create an instance', () => {
    expect(new WorkorderMapEntity()).toBeTruthy();
  });
});
