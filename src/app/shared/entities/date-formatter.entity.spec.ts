import { DateFormatterEntity } from './date-formatter.entity';

describe('DateFormatterEntity', () => {
  it('should create an instance', () => {
    expect(new DateFormatterEntity()).toBeTruthy();
  });
});
