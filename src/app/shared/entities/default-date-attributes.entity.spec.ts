import { DefaultDateAttributesEntity } from './default-date-attributes.entity';

describe('DefaultDateAttributesEntity', () => {
  it('should create an instance', () => {
    expect(new DefaultDateAttributesEntity()).toBeTruthy();
  });
});
