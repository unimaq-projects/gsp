import { Routes } from '@angular/router';
import {WorkorderComponent} from './form-data/workorder/workorder.component';

export const routes: Routes = [
  {path: '', component: WorkorderComponent}
];
